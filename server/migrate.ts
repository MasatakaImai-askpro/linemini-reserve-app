export async function runMigrations() {
  // 今後マイグレーションするときは
  // 1.schemaの更新
  // 2.npx drizzle-kit generate の実行
  // 3．npx drizzle-kit migrate の実行
  // 上記の順序で行うこと
  console.log("Migrations completed.");
}
